namespace BackendNet.Domain;

public enum UserRole { USER, ADMIN }

public enum Gender { MALE, FEMALE }

public enum Religion { ISLAM, CHRISTIAN, CATHOLIC, HINDU, BUDDHA, KONGHUCU }

public enum BloodType { A, B, AB, O }

public enum MaritalStatus { SINGLE, MARRIED, DIVORCED, WIDOWED }

public enum Degree { SD, SMP, SMA, SMK, D1, D2, D3, D4, S1, S2, S3 }
